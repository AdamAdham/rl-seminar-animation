[CLI]
frame_rate = 30
pixel_height = 1080
pixel_width = 1920
background_color = BLACK
background_opacity = 1
scene_names = Default

# Additional configuration (My additions) -------------------------------

# Ensure 16:9 aspect ratio with logical units
frame_width = 14.22  
frame_height = 8  

# Remove unnecessary prints
verbosity = WARNING